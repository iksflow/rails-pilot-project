<h2>Edit Student Info</h2>

<%= form_for :student, url: { action: "update"}, method: :patch do |form| %>
  <%= form.label :name %>:
  <%= form.text_field :name %><br>
  <p>
    <%= form.submit %>
  </p>
<% end %>
<%= link_to "Back to home", schools_path %>
